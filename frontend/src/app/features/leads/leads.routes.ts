import { Routes } from '@angular/router';
import { placeholderRoutes } from '../../core/config/route-helpers';

// Dev A: replace a placeholder by adding a real route with the same `path` BEFORE it.
export const LEADS_ROUTES: Routes = placeholderRoutes('/leads');
