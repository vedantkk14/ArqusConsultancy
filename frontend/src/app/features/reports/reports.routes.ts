import { Routes } from '@angular/router';
import { placeholderRoutes } from '../../core/config/route-helpers';

// Dev C: replace a placeholder by adding a real route with the same `path` BEFORE it.
export const REPORTS_ROUTES: Routes = placeholderRoutes('/reports');
