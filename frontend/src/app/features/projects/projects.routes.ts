import { Routes } from '@angular/router';
import { placeholderRoutes } from '../../core/config/route-helpers';

// Dev B: replace a placeholder by adding a real route with the same `path` BEFORE it.
export const PROJECTS_ROUTES: Routes = placeholderRoutes('/projects');
